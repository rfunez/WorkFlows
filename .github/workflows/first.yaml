name: FirstFlow
on: 
    push: 
        branches: ["develop"]
jobs:
    build:
        runs-on: ubuntu:latest
        steps:
            - name: Greeting
              run: echo "Hi"
            - name: Listing dir
              run: ls -latr
